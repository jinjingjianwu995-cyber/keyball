name: Build a firmware

on:

  workflow_call:
    inputs:
      qmk_version:
        default: '0.22.14'
        type: string
        required: false
      keyboard:
        type: string
        required: true
      keymap:
        type: string
        required: true

jobs:

  build:

    name: Build a firmware ${{ inputs.keyboard }}:${{ inputs.keymap }}

    runs-on: ubuntu-22.04
    container:
      image: ghcr.io/qmk/qmk_cli:latest

    steps:

    - name: Checkout source
      uses: actions/checkout@v4

    - name: Checkout qmk_firmware
      uses: ./.github/actions/checkout-qmk_firmware

    - name: Install a link to own source
      run: ln -s $(pwd)/qmk_firmware/keyboards/keyball __qmk__/keyboards/keyball

    - name: Create venv & install Python requirements
      run: |
        python3 -m venv .venv
        . .venv/bin/activate
        python -m pip install --upgrade pip
        # ▼▼▼ 1. ここに 'qmk' のインストールを追加 ▼▼▼
        pip install qmk
        # ▲▲▲ 追加 ▲▲▲
        pip install -r __qmk__/requirements.txt

    - name: Compile and link
      run: |
        . .venv/bin/activate
        # ▼▼▼ 2. コマンドを 'python3 -m qmk_cli' 経由に変更 ▼▼▼
        python3 -m qmk_cli compile -j 4 -kb keyball/${{ inputs.keyboard }} -km ${{ inputs.keymap }}
        # ▲▲▲ 変更 ▲▲▲

    - name: Archive built firmware
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.keyboard }}-${{ inputs.keymap }}-firmware
        path: __qmk__/*.hex
